apply plugin: 'java'
apply plugin: 'pmd'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'org.testng', name: 'testng', version: '6.+'
}

test {
    useTestNG()
}

pmdMain {
    ruleSets = [ "java-basic", "java-strings", "java-braces" ]
    ignoreFailures = true
}

